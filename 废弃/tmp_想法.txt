循环：
循环结束条件：
到达次数退出（或许应该记专家次数，而不是轮数？）。
用户、或者自动化管理agent觉得可以退出了。

agent或用户可以觉得这次的讨论太烂了，然后重开。

循环过程中：
由用户点名专家发言（如不点名则按顺序）
专家指定哪个专家进行讨论（可以加个容器框，容器框中的专家自动添加一个标签提示词，有程序自动通过正则检测到标签，然后通过标签中的专家名，指定哪个专家发言）
单纯的按顺序发言。


章纲设计这块也不对，其实五个默认专家本质上还是没想到点子上（但这个可以作为参考保留），应该增加一个拆分专家，专门做章节拆分。

另外还有世界书和向量数据库应该在哪里调用。


应该再添加一个世界书agent模块，用双箭头连接，可以设置专家可以选择自己调用，还是当进行到专家的时候就自动调用并插入提示词。而世界书调用更新也是，用户可以设置当专家发言结束之后，自动调用世界书agent模块进行管理。也可以在一定次数后触发，或者是一轮循环之后出触发。
向量数据库也是同理。

可以点击专家之后，在右边配置，配置完成之后，会有图标。

上面应该在浮动一个栏，里面有容器循环之类的图标，就像某些电路设计软件一样。

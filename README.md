# AI辅助网文创作系统

> 将软件工程范式引入网文创作——分层解耦、多专家协作、数据驱动。
>
> 五层瀑布模型 (L1→编排层→L3→L4) + 三元数据支撑 (世界书 + RAG历史 + RAG技法)

---

## 项目现状

| 指标 | 数值 |
|------|------|
| 阶段 | 原型开发，全流程打通 |
| 后端 | ~6200 行 Python，17 个注册模块，73 条 API 路由 |
| 前端 | ~8500 行 Vue 3，10 页面 + Vue Flow 编排画布 |
| 测试 | 37/37 通过 |
| 待完成 | L3/L4 多技能体系、L3 向量映射库、混合检索器集成 |

---

## 快速开始

```bash
git clone <repo> && cd talk
source venv/bin/activate
./start.sh                          # 后端 :7860

cd frontend && npm install && npm run dev  # 前端 :5173

# 配置 API 密钥
编辑 data/user/config.yaml
```

---

## 一、创作模型

传统 AI 写作的根本问题：用户输入模糊创意，指望 AI 一步到位写出正文。这相当于跳过蓝图直接砌砖——没有图纸，AI 只能盲目堆砌。

本系统将创作解耦为多个层级，自顶向下、逐步细化。核心第一性原理是**"故事"与"叙事"分离**——L2 管"讲什么"，L3 管"怎么讲"。

```
L1   种子层      故事愿景     "我要写一个什么样的故事"
编排层           卷纲→章纲    "分成几卷 + 每章功能序列"
L3   叙事层      带标签草稿    "草稿嵌入叙事指令"
L4   渲染层      精修正文     "扩展打磨成终稿"
```

### L1 种子层

| 项目 | 说明 |
|------|------|
| 角色 | 创作顾问 |
| 输入 | 用户模糊创意 |
| 输出 | 《故事愿景文档》(VisionDocument) |
| 实现 | GuidedFormGenerator（聊天+表单双模式） |

**产出内容**：核心梗/脑洞、阅读契约、粗略大纲、核心设定（世界观/主角人设/金手指）、热点元素

### 编排层（原 L1.5 + L2 合并）

L1.5（卷纲规划）和 L2（章纲设计）已合并为统一的**编排层**。专家可在画布上自由拖拽组合，预设模板覆盖所有使用场景。

| 专家 | 职责 | 核心概念 |
|------|------|---------|
| **资深作者** | 创作方向把控 | 热点判断、毒点预警、分卷方案 |
| **读者代表** | 体验审核 | 模拟读者情绪、检测疲劳点，只提问题不提供方案 |
| **剧情架构师** | 故事骨架 | 功能→序列→情节拆解，因果闭环 |
| **网络编辑** | 市场效果 | 黄金三章、压抑-释放、毒点规避 |
| **人物设计师** | 人设一致 | 行动元分配、扁形/圆形人物、OOC 检测 |
| **章节拆分师** | 卷→章翻译 | 将卷纲拆为具体章节号及概要 |
| **讨论总结师** | 讨论收敛 | 提炼共识、标注分歧 |

**三种驱动模式**：

| 模式 | 发言顺序 | 适用场景 |
|------|---------|---------|
| 人物驱动流 | 人物 → 剧情 → 编辑 | 文青文、种田文 |
| 剧情驱动流 | 剧情 → 人物 → 编辑 | 无限流、系统文 |
| 市场驱动流 | 编辑 → 剧情 ↔ 人物 | 跟风热点、商业定制 |

**协作模式**：semi_auto（专家发言后暂停）/ full_auto（全自动）/ manual（用户点名）

**多维功能标注**：每个叙事单位标注四轴——剧情轴（推进/转折/铺垫/阻碍/回收/承接）、人物轴（揭示特性/触发成长/确立关系等）、世界轴（展示设定/营造氛围等）、读者效果（期待感/满足感/代入感/爽感/惊喜感，标↑↓强度）。L2 标注好读者效果，L3 直接查映射表——省去 L3 最不可靠的"推断读者效果"这一步。

**编排画布**：基于 Vue Flow，支持拖拽专家节点、连线定义发言顺序、容器分组、@提及点名、管道模式（DAG 数据流）、输入源节点（批量 .md 文件队列按序处理）。

**流式输出**：专家发言实时 SSE 推送（逐字渲染），支持思考过程（thinking）折叠面板，聊天窗口左侧含文档库和队列进度面板。

### L3 叙事层

| 项目 | 说明 |
|------|------|
| 角色 | 映射编译器 |
| 输入 | 精修大纲（含多维功能标注） |
| 输出 | 章纲/细纲（含视角、节奏、话语模式指令） |
| 实现 | MappingCompiler，规划升级为 TaggedDraftGenerator |

**核心创新——效果到技术的确定性映射**：

网文理论是"效果学"（欲扬先抑、装逼打脸），叙事学是"结构学"（外聚焦、慢速扩述）。L3 充当编译器，将效果指令翻译为技术指令：

| 效果目标 | 视角选择 | 节奏控制 | 话语模式 |
|---------|---------|---------|---------|
| 压抑/铺垫 | 反派内聚焦 | 慢速扩述 | 大量对话+心理描写 |
| 爆发/打脸 | 外聚焦 | 中速等述 | 短句动词密集 |
| 余韵/满足 | 自由间接引语 | 快速概述 | 震惊反应+留白 |

**5 种技能**：初稿生成 / 迭代修正 / 换方向重写 / 保守写作 / 加速模式

### L4 渲染层

| 项目 | 说明 |
|------|------|
| 角色 | 执行者 |
| 输入 | L3 章纲指令 |
| 输出 | 精修正文 |
| 实现 | ConstrainedRenderer（SSE 流式） |

L4 不是自由创作，而是**带约束的文本扩展**。执行约束：视角（只写所见不写所想）、节奏（字数控制）、话语模式（句式结构）。

**7 种技能**：初稿扩展 / 文风调校 / 删改压缩 / 描写增强 / 对话打磨 / 保守修复 / 终稿润色

### 迭代闭环

四层不是不可回退的单行道。L3 发现映射困难可打回编排层重审，编排层发现方向问题可重新编排。每一层 AI 提方案，人做决定。

---

## 二、数据支撑

长篇创作的记忆问题不能靠"全塞进上下文窗口"解决（成本 + 注意力衰减）。

```
世界书 (World Book)         RAG-历史回顾           RAG-技法参考
当前状态管理                过往记忆               写作素材
"主角现在是金丹期"          "老周第45章说过..."     "打脸场景怎么写"
```

三者分工：世界书保证**不犯错**，RAG-历史回顾保证**不遗忘**，RAG-技法参考保证**写得好**。

### 世界书

灵感来自 SillyTavern 的触发词上下文注入系统。

**四层结构**：

| 层 | 内容 | 特性 |
|----|------|------|
| 核心层 | 世界观规则、力量体系、阅读契约 | 永久条目 |
| 活跃层 | 人物状态、活跃伏笔、当前道具、势力状态 | 权重机制，随剧情更新 |
| 归档层 | 已完成序列摘要、已回收伏笔、退场人物 | 压缩为摘要 |
| 索引层 | 条目-章节映射、关系图、触发词索引 | 元数据 |

**关键机制**：
- **版本链非覆写**：`[1-30:筑基]→[31-80:金丹]→[81-今:元婴]`
- **重要性衰减**：引用 +1，10 章未引用 -1，归零归档
- **自动压缩**：已完成序列压缩为摘要
- **世界书管理员 Agent**：每序列完成后自动提取变化、更新条目、冲突检测

### RAG 系统

**双重职能**：历史回顾（检索已写章节，动态增长）和技法参考（映射规则+范例，静态库）。

**混合检索方案**（笔记7原型已验证，300 章 ~25 分钟）：
- **多维度语义切片**：同一文本按 plot/character/emotion/function 四种维度切分
- **文档增强**：每个序列生成 5 视角改写
- **混合得分**：`vector_score × 0.6 + keyword_score × 0.4`
- **双库分离**：向量库存摘要+标签（快速检索），MD 文本库存完整切片

---

## 三、技术栈

| 层 | 技术 |
|----|------|
| 后端 | Python 3.13, FastAPI |
| 前端 | Vue 3 (Composition API), Vue Flow, markdown-it |
| 实时通信 | SSE (Server-Sent Events) |
| 向量数据库 | Chroma（嵌入式） |
| 数据存储 | JSON (核心数据) + SQLite (日志) |
| LLM | OpenAI 兼容接口, BaseLLMProvider 抽象 |

### 模块化插件体系

所有功能组件定义 ABC 抽象接口，通过 `@register_module` 装饰器自动发现。切换实现只需改配置。

```
backend/modules/
├── llm/               OpenAICompat, MockLLM
├── rag/               SimpleVectorRetriever (Chroma)
├── worldbook/         STStyleWorldBook
├── l1/                GuidedFormGenerator
├── l3/                MappingCompiler
├── l4/                ConstrainedRenderer
├── experts/           专家池 (7 位)
├── orchestration/     MeetingEngine 统一编排引擎
└── meeting_protocols/  4 种会议协议
```

---

## 四、API 路由（73 条）

| 类别 | 核心路径 | 说明 |
|------|---------|------|
| 系统 | `/health` `/api/modules` `/api/config` | 健康检查、模块列表、配置 |
| 项目管理 | `/api/projects/` | CRUD + 配置 + 日志 |
| L1 | `/api/projects/{id}/l1/` | 聊天(SSE)、生成愿景、草稿自动保存 |
| 编排 | `/api/projects/{id}/meeting/` | 统一会议(SSE)、反馈、设计保存、预设模板 |
| L1.5(旧) | `/api/projects/{id}/l1_5/` | 向后兼容，委托给 meeting 引擎 |
| L2(旧) | `/api/projects/{id}/l2/` | 向后兼容，委托给 meeting 引擎 |
| L3 | `/api/projects/{id}/l3/` | 生成章纲、章纲管理 |
| L4 | `/api/projects/{id}/l4/` | SSE 流式渲染、获取正文 |
| 世界书 | `/api/projects/{id}/worldbook/` | 条目 CRUD、版本管理、自动处理 |
| RAG | `/api/projects/{id}/rag/` | 语义搜索、索引、统计 |
| 文档库 | `/api/projects/{id}/library/` | 文档 CRUD、导入导出、目录管理、溯源链 |
| 自定义专家 | `/api/experts/` | 创建/删除/获取提示词 |

---

## 五、目录结构

```
talk/
├── backend/                          # Python/FastAPI 后端
│   ├── main.py                       # 入口 + 路由注册
│   ├── core/
│   │   ├── config.py                 # YAML 配置 (Pydantic)
│   │   ├── registry.py              # 插件自动发现 + 注册表
│   │   └── protocols/               # ABC 接口 (9 个)
│   ├── modules/                      # 可替换实现
│   │   ├── llm/ rag/ worldbook/     # 数据层
│   │   ├── l1/ l3/ l4/              # 创作层
│   │   ├── experts/                  # 专家池 (7 位)
│   │   └── orchestration/            # MeetingEngine (~730 行)
│   ├── meeting_protocols/            # 4 种会议协议
│   ├── api/                          # 12 个路由文件
│   ├── services/                     # 独立服务
│   └── tests/                        # pytest (32 测试)
│
├── frontend/                         # Vue 3 前端
│   └── src/
│       ├── App.vue                   # 根组件 + 导航 + 文档侧边栏
│       ├── main.js                   # 路由定义
│       ├── lib/sse.js                # SSE 流包装器
│       ├── pages/                    # 10 个页面
│       └── components/               # 编排画布 + 文档库侧边栏 + 输入源节点
│           ├── OrchestrationCanvas.vue
│           ├── ExpertNode.vue
│           ├── GroupNode.vue
│           ├── InputSourceNode.vue
│           └── library/
│               └── DocumentSidebar.vue
│
├── data/                             # 运行时数据
│   ├── user/config.yaml              # 全局配置
│   └── projects/{id}/               # 项目数据
│
├── hybrid_rag_prototype/             # RAG 原型（已验证）
├── papers/                           # 参考论文 + 论文分析
├── 废弃/                             # 已废弃的旧代码和文档
├── start.sh                          # 一键启动
├── 理论总结.md                        # 完整理论体系
├── 使用与设计思路.md                  # 使用指南 + 设计决策
└── 代码框架.md                        # 开发者参考
```

---

## 六、核心文档索引

| 文档 | 内容 | 何时读 |
|------|------|--------|
| [理论总结.md](理论总结.md) | 完整理论体系（笔记1-17整合） | 理解"为什么这么设计" |
| [使用与设计思路.md](使用与设计思路.md) | 使用指南 + 设计决策 | 了解怎么用、为什么选这个方案 |
| [代码框架.md](代码框架.md) | 开发者参考：目录、API、扩展指南 | 开发和贡献 |
| [papers/论文深度分析.md](papers/论文深度分析.md) | 7 篇论文对比分析 | 了解学术定位 |

---

版本：v4.1  
更新：2026-05-03
